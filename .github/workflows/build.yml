name: Deprecated Build Workflow

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Canonical Workflow Notice
        run: |
          echo "Deprecated: use ci.yml for build validation."
          exit 1

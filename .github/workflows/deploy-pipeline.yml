name: Deploy Pipeline (Deprecated)

# Deprecated: Replaced by deploy-staging.yml and deploy-production.yml.
# Kept for historical reference only and intentionally performs no deployment.

on:
  workflow_dispatch:
    inputs:
      reason:
        description: "Why this deprecated workflow is invoked"
        required: false
        default: "accidental trigger"

jobs:
  deprecated:
    name: Deprecated workflow notice
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "::warning::deploy-pipeline.yml is deprecated and performs no deployment."
          echo "Use deploy-staging.yml (manual) or deploy-production.yml instead."

## 2026-03-05 로컬빌드 및 배포 검증 요약

- **완성본 정리 반영**: 사용자 요청된 병렬 개선 작업을 기준으로 `STATUS` 상태를 최신화.
- **로컬 빌드/기동 시도**: `python start_platform.py`를 통해 8000/9000 구간 응답 확인을 수행.
- **결과(로컬)**: `start_server.py`(8000) 기준 `/health`, `/web/platform/index.html`, `/web/review/index.html`은 200 확인, `/api` 계열은 기대 동작(미인증 401)으로 401 반환. 당시 `/api/health` 500 메모는 있었지만 2026-03-10 재검증에서는 200으로 정정되었다.
- **로그 이슈 포인트**: 초기 앱 초기화 메시지는 확인되었으나 Flask 런타임 리슨 로그/바인딩 로그 미노출.
- **로컬 배포(컨테이너) 시도**: `docker compose up -d --build`는 현재 Docker 데몬 접근/권한 이슈로 실패.
- **차단 원인**: 데몬 소켓 접근 제한 및 Elasticsearch 이미지 조회 실패.
- **다음 액션**: 1) 데몬 권한/실행 복구 후 컨테이너 배포 재시도 2) 단일 프로세스 서버에서 포트 바인딩 로그를 강제 출력 후 헬스체크 재실행 3) 성공 시 주요 화면(`/web/platform/index.html`, `/web/review/index.html`, `/web/bohemian-marketing/index.html`) 재검증

# ?뱢 SoftFactory Platform Status

> **Purpose**: Fact Source: `docs/FACTS_SNAPSHOT_LATEST.json` (generated by `python scripts/generate_facts_snapshot.py`).
> **Status**: ?윟 ACTIVE (愿由?以?
> **Impact**: [Engineering / Operations]

---

## ??Executive Summary (?듭떖 ?붿빟)
- **二쇱슂 ?댁슜**: 蹂?臾몄꽌??SoftFactory Platform Status 愿???듭떖 紐낆꽭 諛?愿由??ъ씤?몃? ?ы븿?⑸땲??
- **?곹깭**: ?꾩옱 理쒖떊???꾨즺 諛?寃????
- **?곌? 臾몄꽌**: [Master Index](./NOTION_MASTER_INDEX.md)

---

Fact Source: `docs/FACTS_SNAPSHOT_LATEST.json` (generated by `python scripts/generate_facts_snapshot.py`).

Updated: 2026-03-04
Status owner: engineering baseline sync

## Canonical Status Snapshot

- Codebase maturity: high
- Documentation consistency: medium (actively being normalized)
- Local run readiness: high
- Production deployment readiness: medium
- Structure governance readability: high (`docs/status/repo-structure-management-dashboard.md`)

## Verified Facts

- Backend framework: Flask + SQLAlchemy + Flask-Migrate
- App factory: `backend/app.py`
- Registered blueprints in app factory: 28
- Route decorators in backend (raw count): 442
- OpenAPI documented paths (`docs/openapi.json`): 62
- Runtime ports in active scripts:
  - `start_server.py` -> 8000
  - `start_platform.py` -> 9000
  - docker-compose API -> 8000
- Health endpoint: `GET /health`
- Admin metrics endpoint: `GET /api/admin/metrics` (auth + admin)

## Delivery State (Practical)

- Functional implementation: mostly complete
- Integration depth: broad
- Test assets: broad (unit/integration/e2e + top-level suites)
- Ops assets: broad (docker, monitoring, workflows)

## Risks Remaining

- Historical markdown reports still contain conflicting completion claims.
- API quick-reference docs previously diverged from real route names in a few areas.
- Multiple deployment workflows exist; teams must use one canonical path per environment.
- Dev/demo fallback secrets remain in parts of config and should be hardened before production.
- Search query payloads for `/api/search` and advanced filters were accepted with weak coercion.
- [완료][2026-03-04] Platform main dashboard JS drift: `web/platform/index.html`의 의존 스크립트 `dashboard-enhanced.js`를 신규 추가해 KPI/차트/알림/내보내기 로직 바인딩이 가능하도록 보강함.
- 2026-03-04 화면 검증에서 확인된 추가 리스크: `http://127.0.0.1:9000/web/platform/index.html`는 응답되나 `dashboard-enhanced.js`는 404, `/api/platform/dashboard`는 미인증 시 401로 사용자 대시보드 데이터 로딩이 실패.

## 2026-03-04 Deployment Validation

- `GET /health` (9000): `200`
- `GET /web/platform/index.html` (9000): `200`
- `GET /web/platform/dashboard-enhanced.js` (9000): `200` (deployed)
- `GET /api/platform/dashboard` (9000): `401` (auth required)

## Current Direction

1. Keep documentation aligned with executable code only.
2. Preserve backward compatibility for existing runtime behavior.
3. Treat changes as non-breaking unless covered by tests and health checks.
4. Use staged rollout and rollback guardrails for deployment changes.

## 2026-02-28 Search Stability Improvements Applied

- `/api/search` and `/api/search/advanced` now enforce safe numeric parsing and index/sort field validation.
- `per_page` is now explicitly bounded in search queries (1~100) and passed through to Elasticsearch pagination.
- Advanced query operators/fields are validated before constructing Elasticsearch boolean queries.
- Similar error paths now avoid leaking internal exception strings while preserving diagnostics in server logs.

## 2026-02-28 MCP Rationalization Applied

- MCP status is now reality-based in `orchestrator/mcp-registry.md` using:
  - `Configured` (in active MCP profiles: `.mcp.json`, `.mcp.core.json`, `.mcp.optional.json`)
  - `Ready` (env key presence)
  - `Used` (executable integration evidence)
- `claude` server mismatch resolved: now explicitly tracked in registry.
- Core recommendation fixed to fetch, filesystem, sqlite.
- Optional and removal-candidate MCPs are now explicitly separated to reduce configuration noise.
- New profile files added:
  - `.mcp.core.json` (minimal)
  - `.mcp.optional.json` (optional/on-demand)

## 2026-03-03 Deployment Runtime Alignment Applied

- Render/Deploy entrypoint mismatch is resolved with `run.py` compatibility:
  - Added root `run.py` for hosts expecting `python run.py`.
  - `run.py` and `start_server.py` now honor `PORT` first, then `APP_PORT` (fallback `8000`).
- `Dockerfile` local health check is now port-variable aware:
  - Uses `APP_PORT`/`PORT` equivalent defaulting to `8000` so runtime overrides are respected.
- `Dockerfile.prod` already remains bound to `APP_PORT` for gunicorn and health checks.

## 2026-02-27 Normalization Applied

- Canonical CI/CD workflows are now fixed to:
  - `ci.yml`
  - `security.yml`
  - `deploy-staging.yml`
  - `deploy-production.yml`
- Legacy duplicate workflows are disabled (manual trigger only, explicit fail message):
  - `deploy.yml`, `build.yml`, `test.yml`, `lint.yml`
- JWT signing now blocks missing `PLATFORM_SECRET_KEY` in production (`backend/auth.py`).
- OAuth redirect URIs no longer silently default to localhost in production (`backend/config.py`).
- `scripts/generate_openapi.py` now generates a real route-derived `docs/openapi.json`.




## 2026-03-10 검증 업데이트

- **`/api/health` 상태 갱신**: Flask test client와 로컬 서버 기준으로 `GET /api/health`가 `200` 응답을 반환하는 것을 재확인했다. 기존 `500` 메모는 최신 코드 기준으로 더 이상 유효하지 않다.
- **로그인 딥링크 복귀 수정**: `web/platform/login.html`에서 `next` 파라미터를 우선 처리하도록 보정했고, 데모 로그인도 동일한 후처리 경로를 사용하도록 수정했다. 이제 `http://127.0.0.1:8000/web/coocook/index.html` 직접 진입 후 로그인하면 다시 CooCook로 복귀한다.
- **CooCook 핵심 흐름 복구**: `web/coocook/index.html`, `web/coocook/shopping-list.html`, `web/coocook/shopping-list-app.js`를 재정리해 허브와 쇼핑리스트 흐름을 정상화했다.
- **브라우저 검증 결과**: Playwright로 `CooCook direct entry -> login -> CooCook hub -> shopping list -> item check` 흐름을 실제로 통과했고, 이 구간에서 콘솔 에러는 재현되지 않았다. 남은 콘솔 경고는 `cdn.tailwindcss.com`의 개발용 경고뿐이다.
- **정적 자산 경로 정리**: `cookie-consent.js` 경로를 상대경로로 맞췄고, `/favicon.ico` 요청은 204로 응답하도록 정리했다.
- **Telegram 연결 플로우 보강**: `/api/telegram/link-account -> /api/telegram/webhook -> /api/telegram/status` 흐름으로 실제 계정 링크가 완료되도록 수정했다. 테스트 기준으로 webhook 링크 완료와 unlink는 정상 동작했고, `TELEGRAM_BOT_TOKEN` 미설정 시 테스트 메시지는 `503`으로 명확히 응답한다.
- **SNS webhook 계약 영속화 완료**: `SNSAutomate`에 `engine`, `webhook_url`, `delivery_mode`, `payload_version`, `requested_at` 컬럼을 추가했고, 생성/조회/수정 API가 해당 값을 실제 DB에 저장/반환하는 것을 확인했다. 잘못된 webhook URL은 서버에서도 `400`으로 차단한다.
- **Docker 복구 준비 작업**: 현재 Windows 서비스 기준 `com.docker.service`가 `Stopped` 상태여서 데몬 자체가 비활성이다. 코드 측면에서는 `docker-compose.yml`에서 Elasticsearch/ops/monitoring 서비스를 optional profile로 분리해, 데몬 복구 후 기본 API 스택(`api`, `postgres`, `redis`)부터 먼저 띄울 수 있게 정리했다.

## 2026-03-17 회귀 검증 업데이트

- **인증 회귀 스크립트 안정화**: [scripts/auth-browser-regression.py](/d:/Project/scripts/auth-browser-regression.py)의 페이지 판정 로직에서 `networkidle` 의존을 제거하고 `load + body attach + short settle` 기준으로 조정했다. 실서비스 화면이 백그라운드 폴링을 수행할 때 정상 렌더 상태를 timeout으로 오판하던 문제를 해소했다.
- **로그인 단계 플래키니스 제거**: 데모 로그인 진입부도 동일하게 `networkidle` 대신 실제 DOM/URL 전환 기준으로 대기하도록 수정했다.
- **배포 회귀 재검증 완료**: `https://softfactory-platform.vercel.app` 대상 브라우저 인증 회귀에서 `platform-dashboard`, `sns-auto-create`, `ai-automation`, `instagram-cardnews` 4개 보호 페이지가 모두 통과했다.
- **최신 증적**: `.workspace/reports/playwright/auth-regression-20260316_134352/report.json`

## 2026-03-17 연속 후속 작업 업데이트

- **Docker 블로커 재확인**: Windows 서비스 `com.docker.service`는 여전히 `Stopped` 상태였다. 컨테이너 기반 검증은 Docker Desktop/daemon 복구 전까지 진행 불가하다.
- **로컬 런타임 재점검**: `start_server.py`는 기동 직후 즉시 응답하지는 않았지만, 지연 후 `http://127.0.0.1:8000/web/review/index.html` `200`, `http://127.0.0.1:8000/api/platform/dashboard` `401`까지 확인해 앱 초기화 후 HTTP 응답 경로는 살아 있음을 재확인했다.
- **통합 테스트 추가 검증 통과**: `test_runtime_database_selection`, `test_route_alias_compatibility`, `test_app_factory_reentry`, `test_growth_event_gateway`, `test_wordpress_publisher_service`, `test_api_endpoints`, `test_coocook_booking_updates`, `test_growth_admin_surfaces`가 모두 통과했다.
- **기술부채 정리**: `backend/services/coocook.py`의 `Booking.query.get()`를 `db.session.get()`으로 교체해 SQLAlchemy legacy 경고를 제거했다.
- **로그 경고 정리**: [backend/logging_config.py](/d:/Project/backend/logging_config.py)에서 deprecated import를 `pythonjsonlogger.json.JsonFormatter`로 교체했다.
- **현재 남은 실질 작업**: Docker daemon 복구 후 `api + postgres + redis` 기본 스택 기동, `/health` 및 `/api/health` 컨테이너 헬스체크, 주요 정적 화면 재검증.

## 2026-03-19 검증 자동화 보강 업데이트

- **로컬 readiness 판정 보강**: [scripts/verify_integration.py](/d:/Project/scripts/verify_integration.py)에 `_is_server_ready()`를 추가하고, readiness 기준을 `/health` 단일 경로에서 `/health` 또는 `/api/health` 응답으로 확장했다.
- **로컬 서버 기동 경로 정리**: 같은 스크립트는 인라인 `app.run(...)` 대신 `start_server.py` 엔트리포인트를 사용하도록 바꿨고, `DEBUG=false`, `FLASK_ENV=development`, `ENVIRONMENT=development`, `TESTING=false`를 강제해 재현성을 높였다.
- **기동 실패 진단 강화**: 서버가 제시간에 뜨지 않으면 subprocess `stdout/stderr` 마지막 구간을 함께 출력하도록 보강했다.
- **UI 스모크 플래키니스 제거**: [scripts/ui-window-smoke-test.py](/d:/Project/scripts/ui-window-smoke-test.py)에서 `networkidle` 대기를 제거하고 `domcontentloaded + load + body attach + short settle` 기준으로 화면 렌더를 판단하도록 수정했다.
- **Windows 스모크 런처 정리**: [scripts/run-ui-window-smoke.ps1](/d:/Project/scripts/run-ui-window-smoke.ps1)을 다시 정리해 `/health`와 `/api/health` 모두를 확인하고, 로컬 서버 대기 시간을 최대 60초로 늘렸다.
- **검증 테스트 추가**: [tests/test_verify_integration_script.py](/d:/Project/tests/test_verify_integration_script.py)를 추가해 readiness fallback과 `start_server.py` subprocess 호출 계약을 고정했다.
- **검증 결과**: `tests/test_verify_integration_script.py`, `tests/integration/test_runtime_database_selection.py`, `tests/integration/test_api_endpoints.py` 모두 통과했고, `python -m py_compile scripts/verify_integration.py scripts/ui-window-smoke-test.py`도 통과했다.

## 2026-03-19 직전 작업 한국어 정리

- **검증 자동화 안정화 완료**: 로컬 서버가 느리게 뜨거나 `/health` 대신 `/api/health`만 먼저 살아나는 상황을 허용하도록 검증 스크립트 기준을 현실에 맞게 조정했다.
- **실행 경로 표준화**: 검증 스크립트가 임시 Flask 인라인 실행이 아니라 실제 운영 진입점인 `start_server.py`를 사용하도록 맞췄다.
- **브라우저 스모크 오탐 감소**: `ui-window-smoke-test.py`에서 `networkidle` 기반 판정을 제거해 백그라운드 폴링이 있는 화면도 정상 렌더 시 성공으로 처리하도록 보정했다.
- **Windows 보조 스크립트 정리**: `run-ui-window-smoke.ps1`의 문자열 깨짐과 짧은 대기시간 문제를 정리했고, 헬스체크 경로를 `/health`와 `/api/health` 둘 다 확인하도록 확장했다.
- **자동화 회귀 방지 장치 추가**: `tests/test_verify_integration_script.py`를 추가해 readiness fallback과 subprocess 실행 계약이 다시 깨지지 않도록 고정했다.
- **현재 의미 있는 남은 과제**: Docker 데몬이 꺼져 있어 컨테이너 기반 검증은 아직 진행하지 못하고 있다. 그 전까지는 로컬 실행 경로와 자동화 검증 품질을 계속 높이는 단계다.

## 2026-03-19 종합 검증 자동화 추가 업데이트

- **verify_integration 병목 제거**: `scripts/verify_integration.py`에서 자식 서버 프로세스의 `stdout/stderr`를 pipe로 잡아두던 구조를 `.workspace/logs/verify_integration_server_<port>.log` 파일 로깅으로 변경했다. 이로 인해 로그 버퍼가 차면서 서버가 멈추는 문제를 해소했다.
- **검증 모드 분리**: `verify_integration.py`에 `--suite core|full` 옵션을 추가해 짧은 핵심 검증과 장시간 전체 검증을 분리했다.
- **로컬 검증 모드 현실화**: 로컬 종합 검증은 `TESTING=true` 서버 + `demo_token` 경로를 사용하도록 조정했다. 이 모드는 통합 테스트와 동일한 전제로 핵심 계약을 빠르게 재검증하는 목적이다.
- **SNS 계정 목록 복구**: [backend/services/sns_auto.py](/d:/Project/backend/services/sns_auto.py)의 `GET /api/sns/accounts`에 legacy SQLite 스키마 fallback을 추가했다. 로컬 DB에 `sns_accounts.site_url` 컬럼이 없어도 최소 필드 조회로 정상 응답하도록 보정했다.
- **핵심 종합 검증 통과**: `python scripts/verify_integration.py --suite core --port 8000 --report-file .workspace/reports/verify_integration_core_20260319.json` 실행 결과 `12/12` 통과, `24.54s`, `100%`였다.
- **자동화 테스트 정합성 유지**: `tests/test_verify_integration_script.py`를 새 파일 로깅 방식에 맞게 갱신했고, `tests/test_verify_integration_script.py` + `tests/integration/test_api_endpoints.py` 합산 `16 passed`를 재확인했다.

## 2026-03-19 직전 작업 한국어 재정리

- **핵심 검증 자동화 복구 완료**: `verify_integration`이 더 이상 로그 pipe 병목으로 멈추지 않도록 수정했고, 핵심 경로만 빠르게 확인하는 `core` 모드를 추가했다.
- **로컬 DB 구버전 스키마 대응**: `GET /api/sns/accounts`가 `sns_accounts.site_url` 컬럼 부재로 500 나던 문제를 legacy fallback으로 복구했다.
- **실행 증적 확보**: `verify_integration core`를 실제로 실행해 `12/12 통과`, `24.54초`, `100%` 결과를 리포트로 남겼다.
- **테스트 회귀 방지 유지**: `verify_integration` 스크립트 테스트와 API 엔드포인트 테스트를 다시 통과시켜 방금 수정한 실행 경로와 fallback 로직이 깨지지 않음을 확인했다.

## 2026-03-19 연속 작업 추가 업데이트

- **SNS legacy fallback 회귀 테스트 추가**: [tests/integration/test_sns_endpoints.py](/d:/Project/tests/integration/test_sns_endpoints.py)에 `GET /api/sns/accounts`가 `site_url` 컬럼 부재 상황에서도 200으로 fallback 되는지 검증하는 테스트를 추가했다.
- **fallback 테스트 실검증 완료**: 해당 테스트를 직접 노드 지정으로 실행해 `1 passed`를 확인했다.
- **verify_integration 추가 단축**: [scripts/verify_integration.py](/d:/Project/scripts/verify_integration.py)의 HTTP 요청 기본 타임아웃을 `4초`로 낮춰, 일부 느린 엔드포인트가 있어도 전체 검증이 과도하게 길어지지 않도록 조정했다.
- **현재 기준 재검증 결과**: `tests/test_verify_integration_script.py` + `tests/integration/test_api_endpoints.py` `16 passed`, `test_get_all_accounts_falls_back_for_legacy_schema` `1 passed`, `verify_integration core` `12/12 passed`.

## 2026-03-19 Playwright 기반 후속 검증 업데이트

- **스킬 기반 검증 흐름 적용**: 이번 라운드는 `project-builder-flow`와 `playwright` 스킬 기준으로 이어서 진행했다. 목표는 로컬 UI를 실제 브라우저로 다시 검증하고 결과를 상태 문서까지 연결하는 것이었다.
- **로컬 서버 지속성 문제 재현**: `start_server.py`를 detached 프로세스로 올리면 초기 `/health`, `/api/health`, `/web/platform/index.html`는 `200`으로 응답했지만, 수십 초 내 프로세스가 종료되어 이후에는 `127.0.0.1:8000` 접근이 `ERR_CONNECTION_REFUSED`로 바뀌었다.
- **Playwright 브라우저 증적 확보**: `playwright-cli` 세션에서 스냅샷을 다시 수집했을 때 페이지 URL이 `chrome-error://chromewebdata/`로 기록됐고, 저장된 스냅샷 `.playwright-cli/page-2026-03-19T01-10-28-723Z.yml`에도 `ERR_CONNECTION_REFUSED`가 남았다. 이는 앱 화면 렌더 실패라기보다 로컬 서버 프로세스 종료 시점과 맞물린 결과다.
- **현재 해석**: 브라우저 자동화 자체보다 로컬 개발 서버의 detached 실행 안정성이 먼저 해결 과제다. HTTP 단일 검증과 `verify_integration core`는 통과하지만, 장시간 UI 세션을 유지할 정도로 로컬 서버가 안정적으로 살아 있지는 않다.
- **즉시 다음 후보 작업**: 배포 URL 기준 Playwright 검증으로 UI 증적을 먼저 확보하거나, 로컬 서버를 지속 실행할 전용 래퍼/서비스 방식으로 런타임을 고정하는 쪽이 다음 현실적 단계다.

## 2026-03-19 전역 UX 속도 개선 업데이트

- **공통 네트워크 체감 속도 개선**: [web/shared-runtime.js](/d:/Project/web/shared-runtime.js)에 `GET` 요청 캐시, 중복 요청 합치기, 상단 진행 바를 추가했다. 같은 데이터를 반복 조회하는 화면은 최근 응답을 즉시 재사용하고 뒤에서 새로 갱신한다.
- **플랫폼 API 전역 최적화**: [web/platform/api.js](/d:/Project/web/platform/api.js)에 플랫폼 전용 `GET` 응답 캐시와 in-flight dedupe를 넣었다. 플랫폼 하위 페이지 전반에서 중복 호출과 새로고침 체감 지연을 줄이는 목적이다.
- **대시보드 첫 화면 응답성 개선**: [web/platform/dashboard-enhanced.js](/d:/Project/web/platform/dashboard-enhanced.js)는 최근 스냅샷을 먼저 렌더한 뒤, `dashboard / paymentHistory / billingInfo`를 병렬 새로고침하도록 바꿨다. 이제 빈 화면 대기보다 "즉시 표시 후 최신화" 패턴으로 동작한다.
- **전역 빠른 이동 추가**: [web/unified-ui.js](/d:/Project/web/unified-ui.js)와 [web/unified-ui.css](/d:/Project/web/unified-ui.css)에 `Ctrl+K` 또는 `/`로 여는 빠른 이동 레이어를 추가했다. 이미 `unified-ui`를 쓰는 화면에서는 주요 링크를 검색 없이 바로 여는 흐름이 기본 제공된다.
- **검증 결과**: `node --check web/shared-runtime.js`, `node --check web/unified-ui.js`, `node --check web/platform/api.js`, `node --check web/platform/dashboard-enhanced.js` 모두 통과했다.

## 2026-03-19 플랫폼 탐색 속도 추가 개선 업데이트

- **플랫폼 공통 빠른 이동 확장**: [web/platform/api.js](/d:/Project/web/platform/api.js)에 플랫폼 전용 quick open 레이어를 추가했다. `api.js`를 로드하는 하위 페이지에서는 별도 HTML 수정 없이 `Ctrl+K`로 링크 검색 후 즉시 이동할 수 있다.
- **페이지 전환 프리패치 추가**: 같은 파일에 내부 HTML 링크 hover/focus 시 문서를 미리 당겨오는 navigation prefetch를 넣었다. 플랫폼 내 화면 이동 체감 지연을 줄이는 목적이다.
- **적용 범위 현실화**: `platform/index.html`, `dashboard.html`, `billing.html`, `integrations.html`, `notifications.html`, `settings.html` 등 `api.js`를 공통 로드하는 플랫폼 페이지 전반이 이번 확장 범위에 포함된다.
- **검증 결과**: `node --check web/platform/api.js` 재검증까지 통과했다.

## 2026-03-19 플랫폼 초기 응답성 추가 보강 업데이트

- **API 모드 감지 중복 제거**: [web/platform/api.js](/d:/Project/web/platform/api.js)에 `detectApiMode()` 메모이즈와 짧은 TTL 캐시를 넣었다. 초기 진입 시 `/health` 판정을 여러 번 반복하지 않고 같은 Promise나 최근 결과를 재사용한다.
- **수동 새로고침만 강제 재검사**: AI 상태 위젯의 수동 새로고침 버튼은 `force` 옵션으로만 재검사하도록 분리했다. 평소 초기 진입은 가볍게, 사용자가 직접 눌렀을 때만 강제 확인하는 구조다.
- **플랫폼 전역 제출 피드백 추가**: 같은 파일에 폼 제출 버튼 pending 상태를 공통으로 묶었다. `api.js`를 쓰는 플랫폼 페이지에서는 제출 직후 버튼이 `처리 중...`으로 전환되고, 네트워크 요청 종료 후 자동 복구된다.
- **검증 결과**: `node --check web/platform/api.js` 재검증까지 계속 통과했다.

## 2026-03-19 저장소 경량화 안전 적용 업데이트

- **ignore 규칙 최소 확장**: [`.gitignore`](/d:/Project/.gitignore)에 `output/`만 추가했다. 현재 이 경로는 WordPress 발행, Playwright 보조 산출물, 생성 브리프 등 로컬 실행 결과가 쌓이는 위치이며 Git 추적 없이도 런타임 동작에는 영향이 없다.
- **안전성 확인**: `git ls-files output` 결과 tracked 파일은 없었고, `git check-ignore -v`로 `output/generated-run/*`, `output/playwright/*`가 새 규칙에 의해 무시되는 것을 확인했다.
- **영향 범위 통제**: `.workspace/`, `.playwright-cli/`, `logs/`, `tmp_logs/`는 원래도 ignore 상태였고, 이번 변경은 새로 떠 있던 `output/`만 경량화 대상으로 편입한 것이다.

## 2026-03-19 일반 화면 반응성 확장 업데이트

- **shared-runtime 범위 확장**: [web/shared-runtime.js](/d:/Project/web/shared-runtime.js)에 내부 HTML 링크 prefetch와 공통 폼 제출 pending 상태를 추가했다.
- **적용 대상**: `analytics`, `audit`, `automation-dashboard`, `chat`, `dashboard`, `operations`, `reports`, `sprints`, `teams`, `growth-automation/*`처럼 `shared-runtime.js`를 공통 로드하는 일반 운영 화면 전반에 이번 개선이 적용된다.
- **체감 변화**: 마우스 오버나 포커스만으로 다음 화면 문서를 미리 당겨오고, 폼 제출 직후 버튼이 `처리 중...`으로 변해 사용자가 대기 상태를 즉시 인지할 수 있다.
- **검증 결과**: `node --check web/shared-runtime.js` 통과.

## 2026-03-19 백그라운드 비용 절감 추가 업데이트

- **네트워크 상황 기반 prefetch 억제**: [web/shared-runtime.js](/d:/Project/web/shared-runtime.js)와 [web/platform/api.js](/d:/Project/web/platform/api.js)에서 `saveData`, `effectiveType`, `document.hidden`을 확인해 느린 네트워크나 숨김 탭에서는 문서 prefetch를 생략하도록 조정했다.
- **대시보드 백그라운드 새로고침 중단**: [web/platform/dashboard-enhanced.js](/d:/Project/web/platform/dashboard-enhanced.js)에 `visibilitychange` 처리를 추가해, 탭이 숨겨진 동안 자동 새로고침 interval을 멈추고 다시 보일 때만 재개하도록 바꿨다.
- **사용자 피드백 안정화**: 일반 런타임 submit pending 라벨은 인코딩 리스크가 없도록 ASCII `Loading...`으로 고정했다.
- **검증 결과**: `node --check web/shared-runtime.js`, `node --check web/platform/api.js`, `node --check web/platform/dashboard-enhanced.js` 모두 통과.

## 2026-03-20 전역 UI/UX 정보 구조 재정렬 구현 업데이트

- **공통 화면 문법 실제 반영**: [web/responsive-framework.css](/d:/Project/web/responsive-framework.css)에 `sf-page-intro`, `sf-primary-action-card`, `sf-status-grid`, `sf-inline-chip` 등 상단 정보 구조용 공통 스타일을 추가했다. 이제 요약, 다음 행동, 상태, 상세 순서를 같은 문법으로 재사용할 수 있다.
- **사이드바 읽기 비용 축소**: [web/platform/api.js](/d:/Project/web/platform/api.js)에서 메뉴 라벨을 더 짧게 정리하고, 링크를 `플랫폼 / 현재 서비스 / 다른 서비스` 기준으로 분류하도록 보정했다. 다른 서비스 링크는 대비를 낮추고 현재 서비스 그룹은 더 강하게 보이게 만들었다.
- **메인 입구 재구성 완료**: [web/index.html](/d:/Project/web/index.html)을 전체 재작성해 `운영 입구` 역할만 남겼다. 상단은 오늘의 우선순위 1개, 핵심 상태 4개, 주요 서비스 진입 6개, 시스템 전체 상태와 운영 메모 순서로 정리했다.
- **플랫폼 홈/대시보드 역할 분리**: [web/platform/index.html](/d:/Project/web/platform/index.html)은 서비스 전환과 오늘의 작업 중심의 `플랫폼 홈`으로 재구성했고, [web/platform/dashboard.html](/d:/Project/web/platform/dashboard.html)은 KPI와 운영 상세 중심의 `운영 대시보드`로 다시 나눴다.
- **서비스 허브 공통 골격 적용**: [web/webapp-builder/index.html](/d:/Project/web/webapp-builder/index.html), [web/sns-auto/index.html](/d:/Project/web/sns-auto/index.html), [web/ai-automation/index.html](/d:/Project/web/ai-automation/index.html), [web/instagram-cardnews/index.html](/d:/Project/web/instagram-cardnews/index.html), [web/review/index.html](/d:/Project/web/review/index.html)에 `요약 → 다음 행동 → 핵심 상태 → 현재 작업판 → 상세 데이터` 구조를 반영했다. 각 서비스는 기존 데이터와 기능은 유지하고 상단 설명과 우선순위만 재배치했다.
- **보강 중인 영역**: [web/coocook/index.html](/d:/Project/web/coocook/index.html)은 title/body 공통 구조는 맞췄고 추가 상단 카피 정리는 이어서 보강할 수 있다. 추천 흐름 중심 정보 구조는 유지하되 상단 문구를 더 짧게 줄이는 작업이 남아 있다.
- **검증 결과**: `node --check web/platform/api.js` 통과. 주요 대상 파일에 `sf-page-intro`와 핵심 ID(`platformRows`, `servicesGrid`, `nextActionPanel`, `publishQueue`, `approvalSection`)가 의도한 위치에 남아 있는 것도 확인했다.

## 2026-03-20 상세 페이지 정보 구조 추가 정리 업데이트

- **상세 페이지 읽기 순서 적용**: [web/sns-auto/create.html](/d:/Project/web/sns-auto/create.html)에 공통 `sf-page-intro`를 추가해 `계정 선택 → 작성 모드 → 미리보기 → 발행` 순서를 상단에서 바로 이해할 수 있게 정리했다. 본문 편집 영역은 그대로 유지하고 상단 문구와 다음 행동만 재배치했다.
- **예약 관리 상세 정리**: [web/sns-auto/schedule.html](/d:/Project/web/sns-auto/schedule.html)에도 같은 상단 문법을 넣어 `주간 현황 → 예약 목록 → 수정/취소 → 새 등록` 순서가 먼저 보이게 바꿨다. 기존 캘린더/목록 구조는 유지하고 상단 설명과 우선순위만 다시 정리했다.
- **리뷰 집계 상세 정리**: [web/review/aggregator.html](/d:/Project/web/review/aggregator.html)에 공통 요약/다음 행동/상태 블록을 추가하고, 서비스 문구를 운영형 표현으로 바꿨다. `PLATINUM`, `크롤러 통합` 같은 표현은 `추천 캠페인 운영`, `통합 집계`처럼 더 직접적인 라벨로 정리했다.
- **CooCook 정규화 완료**: [web/coocook/index.html](/d:/Project/web/coocook/index.html)은 상단 정보 구조뿐 아니라 인라인 스크립트도 다시 정리했다. 기존 인코딩 깨짐 때문에 부분 수정이 불안정했으므로, 동작에 필요한 흐름만 남긴 깨끗한 스크립트로 교체해 화면 구조와 런타임을 함께 복구했다.
- **검증 결과**: `node --check web/platform/api.js` 통과. [web/sns-auto/create.html](/d:/Project/web/sns-auto/create.html), [web/sns-auto/schedule.html](/d:/Project/web/sns-auto/schedule.html), [web/review/aggregator.html](/d:/Project/web/review/aggregator.html), [web/coocook/index.html](/d:/Project/web/coocook/index.html)은 인라인 스크립트 추출 후 `node --check`까지 통과했다.
